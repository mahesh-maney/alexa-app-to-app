==================================================
 Digilux Alexa App-to-App — AWS Live Test Results
 Date       : 2026-08-04 13:07 IST
 Environment: Production (ap-south-1)
 API Base   : https://iot.digilux.co.in/smarthome
 API Gateway: ds6nxf8ac5 (main Digilux account)
 Test User  : stores@digilux.co.in
 Cognito    : ap-south-1_h1o8s7257
 Token type : CLI ID token (USER_PASSWORD_AUTH)
==================================================

── 1. Auth Guard (all 5 endpoints) ─────────────────
  PASS  status — no auth → 401
  PASS  status — bad token → 401
  PASS  startAppToApp — no auth → 401
  PASS  completeAppToApp — no auth → 401
  PASS  unlink — no auth → 401

── 2. Status — Before Linking ───────────────────────
  SKIP  GET /status → 200 with linked field
  SKIP  GET /status — linked=false (user not yet linked)
         Reason: All Alexa endpoints now require smarthome_server scopes.
         CLI tokens (USER_PASSWORD_AUTH) lack these scopes and are correctly rejected.
         Run with a Flutter OAuth access token for full coverage of this section.

── 3. Callback — Public Endpoint ────────────────────
  PASS  callback — error param → HTML error page
  PASS  callback — code+state → HTML success page
  PASS  callback success — deep link has code
  PASS  callback success — Open Digilux App button

── 4. startAppToApp ─────────────────────────────────
  SKIP  startAppToApp → state field
  SKIP  returns codeChallenge
  SKIP  returns redirectUri
  SKIP  state is UUID (36 chars)
  SKIP  codeChallenge is 43 chars (PKCE S256)
  SKIP  redirectUri = iot.digilux.co.in/alexa/callback
         Reason: startAppToApp POST has authorizationScopes smarthome_server/read+write.
         CLI tokens (USER_PASSWORD_AUTH) lack these scopes and are correctly rejected.
         Run with a Flutter OAuth access token for full coverage of this section.

── 5. completeAppToApp — Input Validation ───────────
  SKIP  empty body → 'code is required'
  SKIP  malformed state → 'Invalid state format'
  SKIP  valid UUID state → Lambda processed (LWA rejection expected)
         Reason: completeAppToApp now requires smarthome_server scopes (same as startAppToApp).
         This ensures Flutter uses the same OAuth access token throughout the linking flow.

── 6. Unlink ────────────────────────────────────────
  SKIP  unlink → 404: {"error": "No linked Alexa account found for this user"}
         Reason: unlink requires smarthome_server scopes — CLI token correctly rejected.

── 7. Status — After Tests ──────────────────────────
  SKIP  GET /status — linked=false (confirmed)
         Reason: status requires smarthome_server scopes — CLI token correctly rejected.

── 8. DynamoDB Consistency ──────────────────────────
  SKIP  session in DDB — status=USED
  SKIP  tokens table — no record (user is unlinked)
         Reason: sections 2–7 skipped (CLI token) — no real session was created

── 9. assetlinks.json ───────────────────────────────
  PASS  assetlinks.json — live at iot.digilux.co.in/.well-known/assetlinks.json
  PASS  assetlinks.json — correct prod SHA256 fingerprint (B2:F9:FD...)

==================================================
 Results: 11 passed, 0 failed, 15 skipped
 PASSED (with 15 skipped — run with Flutter OAuth token for full coverage)
==================================================

Full coverage (25/25) expected when Flutter OAuth access token is used.
All authenticated endpoints require smarthome_server scopes (Hosted UI OAuth flow).

Notes
─────
- API Gateway: ds6nxf8ac5 (main Digilux account). All Alexa routes accessible
  under custom domain iot.digilux.co.in. Deployed stage: smarthome (not prod —
  all deployments must target the smarthome stage to take effect on live domain).

- Authorization scopes (IMPORTANT — do NOT remove):
  All 4 authenticated Alexa endpoints require smarthome_server scopes:
    startAppToApp    POST → smarthome_server/read + smarthome_server/write
    completeAppToApp POST → smarthome_server/read + smarthome_server/write
    status           GET  → smarthome_server/read
    unlink           DELETE → smarthome_server/read + smarthome_server/write
  Only Flutter OAuth access tokens (Hosted UI code flow) carry these scopes.
  CLI USER_PASSWORD_AUTH tokens are correctly rejected with 401 — by design.
  Without scopes set, the COGNITO_USER_POOLS authorizer only accepts ID tokens,
  not access tokens — causing 401 for Flutter's AmplifyAuthInterceptor.

- Rate limiting: REMOVED from alexa_start_app_to_app. No per-user session
  count limits. DynamoDB TTL on sessions (10 min) naturally limits orphaned
  PENDING sessions.

- Authorizer: Cognito USER_POOLS (fp1yfy) on pool ap-south-1_h1o8s7257.
  authorizationScopes set on all 4 endpoints → validates access token scopes.

- Token type: Flutter OAuth access tokens (Hosted UI, smarthome_server scopes)
  are required for all authenticated Alexa endpoints. Generate via Cognito
  Hosted UI: ap-south-1h1o8s7257.auth.ap-south-1.amazoncognito.com

- Redirect URI: iot.digilux.co.in/alexa/callback (no /smarthome prefix).
  Lambda serves HTML callback page directly. A root (none) base path mapping
  on the custom domain routes /alexa/callback to API Gateway.

- Android App Links: assetlinks.json live at
  https://iot.digilux.co.in/.well-known/assetlinks.json
  Package: com.digiluxai.smarthomepro
  SHA256 (debug): F3:D7:06:2F:68:DA:1D:D6:13:4B:FA:7B:8F:93:31:76:27:84:C3:02:
                  63:FB:DC:D4:B1:3B:CD:B1:6C:38:31:26
  SHA256 (prod):  B2:F9:FD:20:5D:1D:85:82:0C:D9:7B:32:28:06:70:1D:16:F1:BE:81:
                  BD:76:E3:85:87:C6:84:9F:CC:C5:A4:EC

- iOS Universal Links: apple-app-site-association endpoint created at
  iot.digilux.co.in/.well-known/apple-app-site-association — pending iOS
  Team ID and Bundle ID from Flutter team.

- Alexa Developer Console: redirect URL confirmed as
  https://iot.digilux.co.in/alexa/callback.
  Skill event URL must be set to https://iot.digilux.co.in/alexa/skill-event
  with AlexaSkillEvent.SkillDisabled subscription enabled.

- siteId support: completeAppToApp accepts optional siteId in request body.
  GET /status?siteId= returns per-site linked status from
  digilux_honeywell_user_device_mapping. DELETE /unlink?siteId= clears
  alexaLinked flag for that site.

- Alexa-initiated unlink: POST /alexa/skill-event handles
  AlexaSkillEvent.SkillDisabled events from Amazon. On receipt, deletes LWA
  tokens and sets alexaLinked=false on all user sites. completeAppToApp stores
  amazonUserId (from LWA Profile API) to enable GSI-based reverse lookup
  (amazonUserId-index on digilux_honeywell_alexa_lwa_tokens).

- Full happy path (startAppToApp → Alexa consent → completeAppToApp →
  status → unlink → status) requires real Alexa account + Flutter OAuth token —
  manual QA with Flutter app.
